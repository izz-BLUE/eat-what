-- =====================================================
-- V7: 为 foods.name 添加唯一索引
-- =====================================================
--
-- 说明：
--   1. 添加 uk_foods_name 唯一索引后，后续 migration 可使用
--      INSERT ... ON DUPLICATE KEY UPDATE 按 name 可靠匹配。
--   2. 如果存在重复 name，此 ALTER 将直接失败（预期行为）。
--   3. 此 migration 是菜品数据管线的基础设施，不修改任何菜品数据。
--
-- =====================================================

ALTER TABLE `foods` ADD UNIQUE KEY `uk_foods_name` (`name`);
